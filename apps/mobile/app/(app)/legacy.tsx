// Stub — the Legacy archive (recipes, stories, wisdom, and family heritage)
// gets its full read + write screens in a later chunk. Home links here for
// full members.
import React from "react";
import { ComingSoon } from "@/components/coming-soon";

export default function LegacyScreen() {
  return (
    <ComingSoon
      emoji="🌳"
      title="The Legacy archive is coming"
      body="Your family's recipes, stories, and wisdom will live here soon. For now, you can explore the archive on the web."
    />
  );
}
