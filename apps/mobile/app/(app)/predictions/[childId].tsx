// Stub — Future Predictions (the yearly family word-cloud game, sealed years,
// and the Book of Predictions) arrive in a later chunk. The vault links here.
import React from "react";
import { ComingSoon } from "@/components/coming-soon";

export default function PredictionsScreen() {
  return (
    <ComingSoon
      emoji="🔮"
      title="Future predictions are on the way"
      body="Soon the whole family can guess what a child will grow up to love, then seal it away until they turn 18. For now, you can play along on the web."
    />
  );
}
