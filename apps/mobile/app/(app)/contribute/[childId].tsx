// Stub — the Contribute flow (a gift to a child's Future Fund, with Apple Pay /
// Google Pay) lands in the next chunk. The vault and gift cards link here.
import React from "react";
import { ComingSoon } from "@/components/coming-soon";

export default function ContributeScreen() {
  return (
    <ComingSoon
      emoji="🌳"
      title="Giving is almost ready"
      body="Adding to a child's future from your phone is coming very soon. For now, you can give a gift on the web."
    />
  );
}
